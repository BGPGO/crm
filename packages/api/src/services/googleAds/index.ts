/**
 * Google Ads — placeholder + ingestão de CSV.
 * Fase 1: dados populados manualmente via POST /api/google-ads/upload (JSON).
 * Fase 2 (futuro): integração direta com Google Ads API (requer Dev Token aprovado).
 */
export { getGoogleAdsDaily, getGoogleAdsMTD } from './client';
